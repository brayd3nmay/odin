{
  "name": "odin",
  "version": "0.1.0",
  "description": "Floating Odin assistant for Obsidian with Claude or Codex.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "build:install": "npm run build && node scripts/install.mjs",
    "test": "vitest run"
  },
  "keywords": [],
  "author": "Brayden May",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22",
    "builtin-modules": "^4",
    "esbuild": "^0.25",
    "obsidian": "latest",
    "typescript": "^5.6",
    "vitest": "^2"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "latest",
    "@openai/codex-sdk": "^0.142.4",
    "zod": "^4"
  }
}
